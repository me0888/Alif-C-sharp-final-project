using Erm.DataAccess;

namespace Erm.BusinessLayer;

internal static class Extensions
{
    /* internal static BusinessProcess ToBusinessProcess(this BusinessProcessInfo profileInfo)
        => new()
        {
            Name = profileInfo.Name,
            Domain = profileInfo.Domain
        };

    internal static BusinessProcess ToBusinessProcess(this BusinessProcessUpdateInfo profileInfo)
        => new()
        {
            Name = profileInfo.Name,
            Domain = profileInfo.Domain
        };    

    internal static Risk ToRisk(this RiskInfo riskInfo)
        => new()
        {
            RiskProfileId = riskInfo.RiskProfileId,
            Name = riskInfo.Name,
            Type = riskInfo.Type,
            Description = riskInfo.Description,
            TimeFrame = riskInfo.TimeFrame,
            Status = riskInfo.Status
        };

    internal static Risk ToRisk(this RiskUpdateInfo riskInfo)
        => new()
        {
            RiskProfileId = riskInfo.RiskProfileId,
            Name = riskInfo.Name,
            Type = riskInfo.Type,
            Description = riskInfo.Description,
            TimeFrame = riskInfo.TimeFrame,
            Status = riskInfo.Status
        };    
    */    
}
